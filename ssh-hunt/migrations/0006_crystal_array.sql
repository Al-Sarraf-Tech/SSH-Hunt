-- Crystal Array expansion.
-- Extends the campaign from chapters 1-7 to chapters 1-12.
-- campaign_chapter (SMALLINT) already supports the wider range; no schema
-- changes required.  This migration exists as a version marker so the
-- migration system records the expansion as applied.

SELECT 1;
